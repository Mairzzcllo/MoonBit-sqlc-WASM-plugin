id: S-003
title: Golden 测试扩展：全 PG 类型覆盖验证
priority: P1
type: test
state: todo
description: >
  在 plugin/golden.mbt 中新增测试函数，使用包含所有 PG 类型（bool、float8、bytea、date、
  timestamp、timestamptz、jsonb、numeric、uuid、inet 等）的 GenerateRequest 验证每个类型
  产生正确的 Value 构造函数调用和 decode getter。当前 golden 仅覆盖 int4 和 text，
  无法捕获类型映射退化（type_to_value_constructor 存在但 Value 变体缺失的问题由 S-001 修复，
  此任务确保长期不退化）。
dependencies:
  - id: S-001
    type: hard
  - id: P0-011
    type: hard
lock:
  owner: null
  timestamp: null
  expires_at: null
retry:
  count: 0
  max: 3
created_at: 2026-05-22
updated_at: 2026-05-22
artifacts:
  - type: test
    path: plugin/golden.mbt
references:
  - plugin/golden.mbt
  - plugin/type_map.mbt
  - plugin/query_codegen.mbt
execution_notes: >
  需要构造包含 bool_col: bool, score: float8, data: bytea, created: date, ts: timestamp,
  meta: jsonb 等字段的测试请求。每个字段验证：1) struct 字段类型正确 2) decode getter 正确
  3) Value 构造器名称正确。先实现 S-001 确保 Value 变体存在，否则 golden 产出无法编译。
