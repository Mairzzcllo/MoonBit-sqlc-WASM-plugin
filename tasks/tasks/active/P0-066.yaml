schema_version: "1.0"
id: P0-066
state: todo
deterministic: true
title: "空/无效标识符处理 — 空名称、空类型、空查询名防护"
type: bugfix
priority: P0
description: >
  3 个空/无效标识符边界：查询名为空时 snake_to_pascal("")
  返回 ""，类型名变为空字符串 "Row"，生成无效代码；
  列名为空时字段名为 ""，产生无效 struct 定义；列数据
  类型为空字符串时静默回退到 String。需在 adapter 层
  验证非空性，空值时使用 fallback 或报错。
dependencies: []
owner: ""
acquired_at: null
expires_at: null
retry_count: 0
max_retry: 3
created_at: "2026-05-30"
updated_at: "2026-05-30"
artifacts: []
references:
  - plugin/type_codegen.mbt
  - plugin/query_codegen.mbt
  - plugin/adapter.mbt
execution_notes: >
  Edge cases #31, #35, #36, #53. Add validation in adapter layer:
  - Empty query name → fallback to "query_{n}" 
  - Empty column name → fallback to "field_{n}"
  - Empty column type → use "text" as default type
  - snake_to_pascal("") should return "Empty" or similar
  - capitalize_first("") returns "" — needs guard
