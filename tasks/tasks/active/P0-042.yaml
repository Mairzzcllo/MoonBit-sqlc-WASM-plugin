id: P0-042
title: ":one/:many 解码行数据"
priority: P0
type: fix
state: todo
description: >-
  CRITICAL #1 — build_body 对 OneRow/ManyRows 的固定逻辑是：1) 拼 sql
  let 绑定；2) 调用 db.exec(sql) 返回 Int；3) let _ = 丢弃结果；
  4) 返回 None 或 []。数据库查询结果被完全丢弃。

  修复：1) 改用 db.query(sql)（返回 Array[Row]）；2) 对返回的每条数据
  调用 StructName::decode(row)；3) OneRow 取首条并 Option.wrap，
  ManyRows 遍历全部解码。

  修复文件：plugin/query_codegen.mbt

  依赖 P0-040 (query 路由正确) + P0-041 (decode 方法存在)。

  验证：moon check 0 errors；golden test 验证生成代码包含正确的解码逻辑。
dependencies:
  - id: P0-040
    type: hard
  - id: P0-041
    type: hard
lock:
  owner: null
  timestamp: null
  expires_at: null
retry:
  count: 0
  max: 3
created_at: 2026-05-19
updated_at: 2026-05-19
related: ["#1"]
