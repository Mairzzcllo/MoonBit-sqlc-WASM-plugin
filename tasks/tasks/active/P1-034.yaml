schema_version: "1.0"
id: P1-034
state: todo
deterministic: true
title: "MockDB 事务可配置 (M5)"
type: feature
priority: P1
description: >
  runtime/mock.mbt:59 MockDB 事务行为硬编码为 Err(QueryError("transactions
  not supported"))，测试 Transaction 重载时无法灵活配置。扩展 MockDB::build
  支持自定义 transaction_fn 闭包注入。
dependencies: []
owner: ""
acquired_at: null
expires_at: null
retry_count: 0
max_retry: 3
created_at: "2026-05-28"
updated_at: "2026-05-28"
artifacts:
  - type: implementation
    path: runtime/mock.mbt
    hash: null
references:
  - runtime/mock.mbt
  - runtime/db.mbt
execution_notes: >
  MockDB struct 新增 tx_fn 字段（Option[() -> Result[Transaction, DBError]]）。
  MockDB::build 时如果 tx_fn 为 None 则返回当前硬编码 error。
  已有测试不受影响（不设置 tx_fn = None 维持原行为）。
