schema_version: "1.0"

id: P2-008
state: done
deterministic: true

title: "byte_data_ptr: 添加 Bytes 布局运行时断言"
type: fix
priority: P2
description: >
  byte_data_ptr 硬编码假设 Bytes 内存布局为 [refcount:4][array_header:4][data...]，
  此布局未文档化，未来 MoonBit 工具链更新可能静默变化。需加运行时断言验证
  Bytes 对象 header 字节符合预期模式，不匹配时 panic 提示工具链不兼容。

dependencies: []

owner: ""
acquired_at: null
expires_at: null

retry_count: 0
max_retry: 3

created_at: "2026-05-28"
updated_at: "2026-05-28"

artifacts:
  - type: implementation
    path: plugin/wasi_io.mbt
    hash: null

references:
  - plugin/wasi_io.mbt:42

execution_notes: >
  在 byte_data_ptr 被首次调用时，分配一个已知模式的 Bytes（如 0xDEAD），读取
  header 字节验证 refcount 和 array_header 字段的预期偏移。验证通过后缓存结果；
  不通过则 panic 并提示更新 byte_data_ptr 偏移量。此验证只产生 ~20 字节运行时
  开销。
