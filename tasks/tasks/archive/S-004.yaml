id: S-004
title: Release v0.1.0 初始版本标记
priority: P1
type: infra
state: done
description: >
  为当前代码库创建 v0.1.0 语义化版本标签并推送，触发 .github/workflows/release.yml
  自动发布流程。版本号与 moon.mod.json 中一致（0.1.0）。首次正式发布，包合完整的
  Value enum（S-001）、package_name 支持（S-002）、核心 PG 类型覆盖和相关测试。
  release.yml 已配置 check→test→build→sha256→GitHub Release 流程。
dependencies:
  - id: S-001
    type: hard
  - id: S-002
    type: hard
  - id: S-003
    type: soft
lock:
  owner: null
  timestamp: null
  expires_at: null
retry:
  count: 0
  max: 3
created_at: 2026-05-22
updated_at: 2026-05-22
references:
  - .github/workflows/release.yml
  - moon.mod.json
execution_notes: >
  S-003 是 soft 依赖——release 可以没有全类型 golden 测试，但生产质量建议前序子任务
  全部完成后再 tag。命令: git tag v0.1.0 && git push origin v0.1.0。
  注意当前无任何 tag，release workflow 首次触发需确认 GitHub PAT 有 contents: write 权限。
