schema_version: "1.0"
id: P1-053
state: done
deterministic: true
title: "移除 query_parameter_limit 死代码 — 或实现参数拆分逻辑"
type: cleanup
priority: P1
description: >
  query_parameter_limit 在 adapter.mbt 中被解析并存入 PluginOptions，
  传入 generate_sources/generate_source 但从未传递给 generate_query_fns。
  generate_query_fns 的签名中根本没有此参数。IR 层也不包含此字段。
  main.mbt:157-163 和 main.mbt:188-194 有两处完全相同的 TODO 注释
  解释"未消费"原因。实现真正的参数拆分需要 SQL 级查询改写，
  工作量中等。
  选项 A：移除 options 解析、struct 字段和 TODO 注释（清理死代码）
  选项 B：实现参数拆分的完整功能
dependencies: []
owner: ""
acquired_at: null
expires_at: null
retry_count: 0
max_retry: 3
created_at: "2026-06-02"
updated_at: "2026-06-04"
artifacts:
  - plugin/adapter.mbt
  - plugin/main.mbt
  - plugin/query_codegen.mbt
  - README.md
references:
  - plugin/adapter.mbt:169,205,224,331-336,398
  - plugin/main.mbt:44,157-163,188-194
execution_notes: >
  推荐选项 A（清理死代码）：

  实现步骤：
  1. 从 PluginOptions 移除 query_parameter_limit 字段
  2. 从 parse_plugin_options 移除解析逻辑（snake + kebab 两处）
  3. 从 generate_sources/generate_source 移除字段传递
  4. 移除两处 TODO 注释
  5. 移除 README 中 query_parameter_limit 的文档行
  6. 移除单元测试中 query_parameter_limit 的测试用例（约 4 个）

  选项 B 未来可在需要时重新引入，但不阻塞当前版本。
  验证：moon build --target wasm 编译通过，moon test 全部通过。

  === FINAL COMPLETION (2026-06-04) ===
  选项 A 已实施：PluginOptions 移除 query_parameter_limit 字段。
  parse_plugin_options 移除 snake_case 和 kebab-case 两处解析分支。
  generate_sources/generate_source 移除字段传递。
  main.mbt 中两处 TODO 注释已删除。
  README.md 中 query_parameter_limit 文档行已移除。
  移除约 4 个相关单元测试。moon test 880/880 pass。
