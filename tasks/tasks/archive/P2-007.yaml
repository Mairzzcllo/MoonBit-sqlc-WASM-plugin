schema_version: "1.0"

id: P2-007
state: done
deterministic: true

title: "generate_types: 重构为不可变返回风格"
type: refactor
priority: P2
description: >
  generate_types_from_catalog、generate_types_from_queries、generate_types
  均通过 items.push() 在可变数组上累积结果，且 generate_types 依赖前两者
  返回的数组不被外部修改。重构为纯函数风格，每步返回不可变数组，消除
  基于约定的隐式不变性假设。

dependencies: []

owner: ""
acquired_at: null
expires_at: null

retry_count: 0
max_retry: 3

created_at: "2026-05-28"
updated_at: "2026-05-28"

artifacts:
  - type: implementation
    path: plugin/type_codegen.mbt
    hash: null

references:
  - plugin/type_codegen.mbt:83

execution_notes: >
  重构不影响外部行为，golden 测试不应变化。可用 Array::concat 或
  Array::append 替代 push 链。MoonBit 的 ImmutableArray 非必需，
  返回新分配的 Array 即可满足要求。
