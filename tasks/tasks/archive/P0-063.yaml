schema_version: "1.0"
id: P0-063
state: done
deterministic: true
title: "字段解码按列名而非索引 — 列顺序变化静默错位"
type: bugfix
priority: P0
description: >
  生成的 decode 方法按列索引顺序（get_int64(0),
  get_string(1), ...）读取 Row，而非按列名匹配。
  如果 sqlc 更改查询列顺序（例如 SELECT 子句顺序变化），
  生成代码静默将值映射到错误字段。需改为按列名解码
  或在 decode 中验证列元数据与字段对应关系。
dependencies: []
owner: ""
acquired_at: null
expires_at: null
retry_count: 2
max_retry: 3
created_at: "2026-05-30"
updated_at: "2026-06-01"
artifacts:
  - runtime/row.mbt
  - plugin/type_codegen.mbt
  - tests/integration/basic/generated.mbt
  - tests/integration/wasm/generated.mbt
references:
  - plugin/query_codegen.mbt
  - runtime/row.mbt
execution_notes: >
  Final (round 2, coder→tester→reviewer): Row 新增 col_names: Array[String] 字段。
  AST FieldDef 新增 col_name: Option[String] 字段，由 adapter 层从 protobuf column name 填充。
  type_codegen.mbt build_decode_body 生成 row.index_of("column_name") 调用（使用原始数据库列名而非转义后 MoonBit 字段名）。
  index_of 返回 Result[Int, DBError] 而非 Int（-1 时返回 TypeError 错误）。
  集成测试 stubs 更新（tests/integration/{basic,wasm}/generated.mbt）。
  moon check 0 errors, moon test 682/682 passed.
